# Spikes detection

Finds **spikes** by threshold and draws them on EEG channels.

- `Run` computes spikes for current sweep **based on selected filters**.
- Data is saved as `$SWEEP_IDX.spikes`.
